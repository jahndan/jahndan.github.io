#include "complex.h"

struct complex complex_add(struct complex lhs, struct complex rhs) {
  (void)lhs; // DELETE THIS LINE (does nothing, just avoids unused var warnings)
  (void)rhs; // DELETE THIS LINE (does nothing, just avoids unused var warnings)

  // try working this out yourself
  struct complex out = {
      // .re = ?,
      // .im = ?,
  };
  return out;
}

struct complex complex_add_id() {
  struct complex out = {
      // .re = ?,
      // .im = ?,
  };
  return out;
}

struct complex complex_add_inv(struct complex c) {
  (void)c; // DELETE THIS LINE (does nothing, just avoids unused var warnings)

  struct complex out = {
      // .re = ?,
      // .im = ?,
  };
  return out;
}

struct complex complex_mul(struct complex lhs, struct complex rhs) {
  (void)lhs; // DELETE THIS LINE (does nothing, just avoids unused var warnings)
  (void)rhs; // DELETE THIS LINE (does nothing, just avoids unused var warnings)

  // try working this out yourself
  struct complex out = {
      // .re = ?,
      // .im = ?,
  };
  return out;
}

struct complex complex_mul_id() {
  struct complex out = {
      // .re = ?,
      // .im = ?,
  };
  return out;
}

struct complex complex_mul_inv(struct complex c) {
  (void)c; // DELETE THIS LINE (does nothing, just avoids unused var warnings)

  struct complex out = {
      // .re = ?,
      // .im = ?,
  };
  return out;
}
